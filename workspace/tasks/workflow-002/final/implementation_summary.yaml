task_id: workflow-002
title: 端到端闭环运行策略产品化
status: waiting_for_human_merge_approval
goal: 将端到端命令的重复运行、跳过已完成任务、失败恢复和输出摘要策略产品化。
completed_scope:
  - CLI 增加 --dry-run，用于预览端到端运行计划且不写入产物。
  - CLI 增加 --rerun-policy，支持 new_ids、skip_completed 和 force。
  - decision_summary.yaml 增加 run_strategy、execution_summary 和 retry_plan。
  - execution_summary 明确区分 completed、skipped、failed 和 next。
  - skip_completed 可复用已完成的 validation、dispatch 和 review 子流程。
  - run_local_task.py 支持按当前任务 ID 推导 dispatch/review 输入路径，解除 workflow-001 硬编码。
  - 下一步兜底推荐会根据当前 workflow 编号递增，避免推荐当前任务本身。
changed_files:
  - scripts/run_end_to_end.py
  - scripts/run_local_task.py
  - config/pipeline.yaml
  - tests/test_run_end_to_end.py
  - workspace/tasks/workflow-002/final/decision_summary.yaml
validation_result:
  dry_run_command: source .venv/bin/activate && python scripts/run_end_to_end.py --task-id workflow-002 --dry-run --rerun-policy skip_completed --json
  run_command: source .venv/bin/activate && python scripts/run_end_to_end.py --task-id workflow-002 --rerun-policy skip_completed --json
  pytest: passed
  pytest_count: 75
  validation_status: passed
  retry_plan_status: not_required
  next_recommended_task: workflow-003
out_of_scope_confirmed:
  - 未自动 PR 或 merge。
  - 未调用外部服务。
  - 未删除用户文件。
next_recommended_task: workflow-003
